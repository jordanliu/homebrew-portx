# PortX

Homebrew tap for [PortX](https://github.com/jordanliu/portx).

## Install

```sh
brew tap jordanliu/portx
brew install portx
```

The formula remains intentionally unavailable until the first PortX release is
published and the release workflow opens the update PR with the real archive
SHA256. The placeholder checksum is never replaced by hand.
